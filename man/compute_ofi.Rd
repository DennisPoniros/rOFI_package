% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ofi.R
\name{compute_ofi}
\alias{compute_ofi}
\title{Compute Order-Flow Imbalance metrics}
\usage{
compute_ofi(
  data,
  window = "1 min",
  rolling = NULL,
  n_ticks = NULL,
  price_weighted = FALSE,
  eps = 1e-08
)
}
\arguments{
\item{data}{A tibble with columns: timestamp, side, size, and optionally price}

\item{window}{Character string for calendar windows (e.g., "1 min", "5 min", "1 hour")}

\item{rolling}{A lubridate period object for rolling windows (e.g., lubridate::dseconds(60))}

\item{n_ticks}{Integer for event-count based windows}

\item{price_weighted}{Logical, whether to compute price-weighted OFI (requires price column)}

\item{eps}{Small value to prevent division by zero in ratios (default: 1e-8)}
}
\value{
A tibble with OFI metrics for each time window:
\itemize{
\item window_start: Start of the time window
\item ofi: Simple order-flow imbalance (buy_size - sell_size)
\item oir: Order imbalance ratio (buy - sell)/(buy + sell + eps)
\item ofi_cum: Cumulative OFI
\item B: Total buy volume
\item S: Total sell volume
\item vol_total: Total volume (B + S)
\item n_trades: Number of trades in window
\item ofi_dollar: Dollar-weighted OFI (if price available)
}
}
\description{
Calculates various order-flow imbalance (OFI) metrics from standardized trade data.
Supports calendar-based windows, rolling windows, or event-count windows.
}
\details{
Only one windowing method can be specified at a time (window, rolling, or n_ticks).

Calendar windows use lubridate::floor_date() for consistent bucketing.
Rolling windows calculate metrics for overlapping periods.
Event-count windows group by fixed number of trades regardless of time.
}
\examples{
# Generate sample data
trades <- simulate_orders(n = 1000, seed = 123)

# Calculate OFI with 1-minute windows
ofi_1min <- compute_ofi(trades, window = "1 min")

# Calculate with 5-minute windows
ofi_5min <- compute_ofi(trades, window = "5 min")

# Rolling 60-second windows
ofi_rolling <- compute_ofi(trades, rolling = lubridate::dseconds(60))

# Event-based windows (every 50 trades)
ofi_ticks <- compute_ofi(trades, n_ticks = 50)
}
