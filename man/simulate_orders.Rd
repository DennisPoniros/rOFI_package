% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_orders.R
\name{simulate_orders}
\alias{simulate_orders}
\title{Simulate synthetic order flow data}
\usage{
simulate_orders(
  n = 10000,
  start = Sys.time(),
  lambda = 5,
  imb = 0.1,
  price0 = 100,
  drift = 0,
  vol = 0.5,
  tz = "UTC",
  seed = NULL
)
}
\arguments{
\item{n}{Number of trade events to generate (default: 10000)}

\item{start}{Starting timestamp (default: Sys.time())}

\item{lambda}{Expected events per minute (Poisson arrival rate, default: 5)}

\item{imb}{Target mean imbalance between -1 and 1 (default: 0.1, slight buy bias)}

\item{price0}{Initial price (default: 100)}

\item{drift}{Price drift per minute (default: 0)}

\item{vol}{Price volatility as percentage (default: 0.5)}

\item{tz}{Timezone for timestamps (default: "UTC")}

\item{seed}{Random seed for reproducibility (default: NULL)}
}
\value{
A tibble with columns: timestamp, side, size, price
}
\description{
Generates synthetic trade-level data with configurable arrival rates,
buy/sell imbalance, and price dynamics. Useful for testing, examples,
and educational demonstrations.
}
\details{
Trade arrivals follow a Poisson process with rate lambda per minute.
Trade sides are determined by the imbalance parameter:
\itemize{
\item imb = 0: equal probability of buys and sells
\item imb > 0: more buys than sells
\item imb < 0: more sells than buys
}

Trade sizes are drawn from a log-normal distribution.
Prices follow a simple random walk with optional drift and volatility.
}
\examples{
# Generate balanced order flow
trades <- simulate_orders(n = 1000, imb = 0, seed = 123)

# Generate order flow with buy pressure
bullish_trades <- simulate_orders(n = 1000, imb = 0.3, drift = 0.1)

# High-frequency data (50 trades per minute)
hft_trades <- simulate_orders(n = 5000, lambda = 50)
}
