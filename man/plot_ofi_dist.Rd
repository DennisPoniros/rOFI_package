% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ofi.R
\name{plot_ofi_dist}
\alias{plot_ofi_dist}
\title{Plot distribution of OFI metrics}
\usage{
plot_ofi_dist(
  ofi_tbl,
  metric = "oir",
  plot_type = c("histogram", "density"),
  bins = 30
)
}
\arguments{
\item{ofi_tbl}{A tibble from compute_ofi() containing OFI metrics}

\item{metric}{Character, which metric to plot (default: "oir")}

\item{plot_type}{Character, "histogram" or "density" (default: "histogram")}

\item{bins}{Number of bins for histogram (default: 30)}
}
\value{
A ggplot2 object
}
\description{
Creates histograms or density plots showing the distribution of OFI metrics.
}
\examples{
trades <- simulate_orders(n = 1000, seed = 123)
ofi <- compute_ofi(trades, window = "1 min")

# Distribution of imbalance ratios
plot_ofi_dist(ofi, metric = "oir")

# Density plot of OFI values
plot_ofi_dist(ofi, metric = "ofi", plot_type = "density")
}
