% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ofi.R
\name{plot_ofi}
\alias{plot_ofi}
\title{Plot Order-Flow Imbalance metrics}
\usage{
plot_ofi(
  ofi_tbl,
  which = c("ofi", "oir", "ofi_cum"),
  ref_line = 0,
  facet = TRUE,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{ofi_tbl}{A tibble from compute_ofi() containing OFI metrics}

\item{which}{Character vector of metrics to plot (default: c("ofi", "oir", "ofi_cum"))}

\item{ref_line}{Numeric value for horizontal reference line (default: 0)}

\item{facet}{Logical, whether to use facets for multiple metrics (default: TRUE)}

\item{title}{Plot title (optional)}

\item{subtitle}{Plot subtitle (optional)}
}
\value{
A ggplot2 object
}
\description{
Creates time-series plots of OFI metrics with sensible defaults
and customizable options. Supports multiple metrics in a single plot
or faceted display.
}
\details{
Available metrics to plot:
\itemize{
\item "ofi": Order-flow imbalance
\item "oir": Order imbalance ratio
\item "ofi_cum": Cumulative OFI
\item "vol_total": Total volume
\item "B": Buy volume
\item "S": Sell volume
\item "ofi_dollar": Dollar-weighted OFI (if available)
}
}
\examples{
# Generate data and compute OFI
trades <- simulate_orders(n = 1000, seed = 123)
ofi <- compute_ofi(trades, window = "1 min")

# Plot default metrics
plot_ofi(ofi)

# Plot only OFI and volume
plot_ofi(ofi, which = c("ofi", "vol_total"))

# Plot without facets
plot_ofi(ofi, which = "ofi", facet = FALSE)
}
