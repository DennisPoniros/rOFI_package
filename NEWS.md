# rOFI 0.1.0

* Initial release
* Added `compute_ofi()` function for calculating order-flow imbalance metrics
* Added `plot_ofi()` function for time-series visualization
* Added `plot_ofi_dist()` function for distribution analysis
* Added `simulate_orders()` function for generating synthetic trade data
* Added `as_ofi()` function for data standardization
* Added comprehensive vignette with examples
* Added complete test suite with ~100 unit tests
